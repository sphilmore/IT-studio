import { Routes } from '@angular/router';
import { FlashCardsComponent } from './flash-cards.component';

export const FLASH_CARDS_ROUTES: Routes = [
  {
    path: '',
    component: FlashCardsComponent
  }
]; 